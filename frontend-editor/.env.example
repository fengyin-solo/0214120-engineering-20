# MD Live Editor 环境变量基线
#
# 当前应用（Vue 3 + Vite）不需要任何环境变量即可运行：
#   npm run dev / npm run build / npm run preview
#
# 登记规则（由 scripts/baseline-check.sh 强制校验）：
# 1. 新增环境变量时，必须先在本文件登记“变量名=”；Vite 仅向客户端代码暴露 VITE_ 前缀变量。
# 2. .env / .env.local / .env.*.local 中出现、但未在本文件登记的变量属于“未知变量”，检查直接失败。
# 3. .env / .env.local / .env.*.local 已被 .gitignore 忽略，切勿提交真实密钥。
#
# 示例（当前无生效变量，仅演示登记格式，取消注释即视为应用声明了该变量）：
# VITE_APP_TITLE=Mira
