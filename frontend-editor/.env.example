# MD Live Editor — 环境配置基线
# 复制为 .env 后按需修改；.env 已被 .gitignore 忽略，不会入库。
# 仅以下变量受支持。npm run check 会对 .env 与进程环境中的
# 未知 VITE_* 变量报错，避免出现“设置了却不生效”的隐性问题。

# 开发服务器监听地址（npm run dev）
VITE_DEV_HOST=0.0.0.0

# 开发服务器端口（npm run dev）
VITE_DEV_PORT=5173

# 构建产物预览端口（npm run preview）
VITE_PREVIEW_PORT=4173
